#include <iostream>


int bw1(){
    
    int matrixLen = sizeof(matrix) / sizeof(matrix[0]);
    int arrayLen = sizeof(metrix[0]) / sizeof(matrix[0][0]);
    if (matrixLen <=2 ||arrayLen <= 2) return 0;
    for(int i = 0; i<= matrixLen; i+=2){
        for(int j =0; j<arrayLen; j++) {
             
        
        }
    }
}
int main(){



}
